import styled from 'styled-components'

import { ContentGrid, dark_bg1 } from '../../styles'

export const StylesChatNav = styled(ContentGrid)`
  position: fixed;
  height: 100%;
  width: inherit;
  background: ${dark_bg1};
  color: #fff;
`
